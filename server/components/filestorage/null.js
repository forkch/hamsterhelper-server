
var os = require('os');
var path = require('path');
var fs = require('fs');

module.exports = {
  store: function(file, filename, finishFunction) {
    console.log('fake storing: ' + filename);

  }
}